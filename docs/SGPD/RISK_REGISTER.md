# Registro de Riscos

Riscos mitigados permanecem na matriz enquanto o controle precisar continuar
ativo. Riscos que deixaram de existir são movidos para a seção de encerrados.

| ID | Risco | Probabilidade | Impacto | Mitigação |
|---|---|---:|---:|---|
| R01 | Acoplamento às tabelas internas do Senior | Alta | Alto | Contrato SQL homologado, objetos qualificados, testes de contrato e revisão a cada upgrade |
| R02 | Uso do owner `SGPD` pela aplicação ampliar impacto de falha ou credencial comprometida | Decisão aceita, estendida ao PRD em 2026-08-05 | Muito alto | `.env` 600, SQL centralizado, sem DDL no runtime, migrations revisadas e auditoria. A reavaliação prevista aconteceu na ADR-055: como o PRD reusa o schema do DEV, criar agora um usuário de aplicação separado do owner mudaria o dono dos objetos no meio do corte. Fica como pendência com prazo (`ENVIRONMENT.md` §7), não como bloqueio de go-live |
| R03 | Dados de colaborador alterados após abertura | Mitigado em 2026-07-29 | Alto | Releitura da chave completa e snapshot imutável criado atomicamente com a abertura; atualização e exclusão rejeitadas |
| R04 | Checklist alterado afetar histórico | Mitigado em 2026-07-29 | Alto | Cabeçalhos estáveis, versões imutáveis, vínculo do grupo à versão exata e snapshot de cada pergunta na tarefa |
| R05 | Desconto indevido | Média | Muito alto | Pretensão, aprovação e segregação |
| R06 | Acesso indevido a documentos médicos | Média | Muito alto | Autorização por classe e setor |
| R07 | E-mails não enviados | Mitigado em 2026-07-31 | Médio | Outbox durável no Oracle gravado na transação do domínio, tentativas append-only com backoff, reabertura do que fica preso em envio, painel de falhas e reprocessamento auditado; permanece o resíduo de o agendamento do sistema operacional parar sem alarme (R63) |
| R08 | Responsável ausente | Mitigado no início em 2026-07-29 | Médio | Múltiplos responsáveis de igual autoridade, validade explícita e bloqueio transacional do início quando setor obrigatório não possuir vínculo efetivo no escopo; escalada posterior sem proprietário individual da tarefa |
| R09 | Processo travado por regra mal configurada | Média | Alto | Prévia do rascunho; exigência de versões publicadas, setor ativo e ao menos um setor obrigatório; conflito explícito quando grupos apontam templates diferentes; regras automáticas adicionais dependem de homologação |
| R10 | Duplicidade de processo | Mitigado na abertura em 2026-07-29 | Alto | Validação sob lock e `ACTIVE_EMPLOYEE_KEY` única no Oracle enquanto o processo não estiver encerrado; cancelamento e encerramento futuros liberarão a chave somente por service auditado |
| R11 | Indisponibilidade ou lentidão do Senior bloquear novas pesquisas | Média | Alto | Timeout, paginação, filtros, health check e erro explícito sem usar dados obsoletos |
| R12 | Migration com lock no Oracle | Média | Alto | Revisão do SQL e dos identificadores Oracle, janela de mudança e validação de constraints/índices; `templates_engine.0002` removeu FK/coluna somente após confirmar zero templates e regras no DEV |
| R13 | Auditoria incompleta | Média | Alto | Service de auditoria e testes |
| R14 | Upload malicioso | Média | Alto | Validação de tipo e tamanho, nomes aleatórios e storage privado |
| R15 | Escopo crescer antes do MVP | Alta | Alto | Roadmap e checkpoints |
| R16 | Divergência futura da versão Python homologada | Baixa | Médio | Python 3.13 fixado no `pyproject.toml`, ambiente isolado e `uv.lock` versionado |
| R18 | Criação futura de HML ou PRD sem paridade com DEV | Baixa | Alto | Exigir nova decisão e inventário antes de ampliar ambientes |
| R19 | Exposição do `.env` local com credenciais | Média | Muito alto | Ignorar no Git, restringir permissões e nunca registrar valores em logs |
| R20 | Ferramentas Python globais com versões divergentes | Alta | Médio | Usar ambiente virtual e lockfile do projeto |
| R21 | Vinculação ao AD associar a identidade errada ou duplicada | Mitigado em 2026-07-28 | Muito alto | Identidade pesquisada e revalidada por `objectGUID`, constraint única, confirmação administrativa, motivo operacional padronizado, auditoria e testes de duplicidade/conflito |
| R22 | Uso indevido do WhiteNoise para evidências ou uploads | Média | Muito alto | Restringir WhiteNoise a estáticos e usar storage privado para evidências |
| R23 | Ausência de CI/CD permitir validações locais inconsistentes | Média | Médio | Padronizar comandos locais e registrar evidências de testes |
| R25 | SMTP AUTH ou permissão de remetente bloquearem notificações | Mitigado em 2026-07-28 | Alto | SMTP AUTH e `Send As` validados com mensagem de prova aceita pelo Microsoft 365 |
| R26 | Owner `VETORH` ser usado indevidamente pela aplicação | Baixa | Muito alto | Configurar somente `SGPD` no runtime e manter `VETORH` restrito a consultas administrativas explícitas |
| R27 | Consulta direta expor CPF completo ou dados excessivos | Média | Muito alto | Excluir CPF das listagens, aplicar autorização por finalidade e manter projeções mínimas |
| R28 | `INNER JOIN` omitir colaborador com cadastro relacionado incompleto | Mitigado em 2026-07-28 | Alto | `LEFT JOIN` de centro de custo homologado por reconciliação global; contrato e contagens permanecem testados |
| R29 | Senha local continuar ativa indevidamente após ativação do login AD | Mitigado em 2026-07-28 | Alto | Backend local, redefinição administrativa, troca da própria senha e SPA usam a mesma política; contas comuns vinculadas são bloqueadas quando AD está ativo; importação usa senha inutilizável; fallback limitado a superusuário de contingência configurável e testado |
| R30 | Migrations não poderem criar objetos no schema `SGPD` | Mitigado em 2026-07-27 | Alto | `CREATE TABLE` e `CREATE SEQUENCE` sem `ADMIN OPTION`, quota de 500 MB e revisão do SQL; 25 migrations aplicadas e validadas |
| R31 | Oracle Client ausente ou incompatível impedir conexão em modo Thick | Baixa | Alto | Fixar Instant Client 19.28 no DEV, validar no readiness e falhar na inicialização sem expor segredo |
| R32 | Usuário autenticado consultar referências fora do escopo de empresa/filial | Mitigado em 2026-07-27 | Alto | Permissão `query_senior_references`, atribuição com validade e escopo, filtro de empresas e resposta `403`; CPF ausente das listagens |
| R33 | Vínculo AD administrativo ser confundido com autenticação AD homologada | Mitigado em 2026-07-28 | Alto | Chaves independentes para descoberta e autenticação, status explícito na API/UI, system check e comando de probe; ativação real permanece pendente e declarada |
| R35 | Chamada direta de service administrativo contornar a autorização do endpoint | Mitigado em 2026-07-28 | Muito alto | Permissão validada no próprio service e testes de negação sem mutação ou auditoria espúria |
| R36 | Operação em lote do ORM alterar ou excluir auditoria append-only | Mitigado em 2026-07-28 | Muito alto | `QuerySet.update()` e `QuerySet.delete()` bloqueados, além das proteções por instância |
| R37 | Desativações concorrentes removerem todos os superusuários ativos | Mitigado em 2026-07-28 | Alto | Lock pessimista dos superusuários ativos em ordem determinística e validação transacional do último ativo |
| R38 | Ampliação da superfície de API na migração introduzir endpoint sem autorização | Mitigado em 2026-07-28 | Muito alto | Endpoints são casca fina sobre services que revalidam permissão; cada endpoint possui teste de permissão negada |
| R39 | Troca obrigatória de senha temporária deixar de ser imposta na SPA/API | Mitigado em 2026-07-28 | Alto | Middleware devolve `403` tipado sob `/api/`, redireciona navegação direta para `/fe/senha` e possui testes dedicados |
| R41 | Portar SCSS desktop first do projeto de referência e inviabilizar o uso em telefone | Mitigado em 2026-07-28 | Médio | Consultas `max-width` proibidas em código novo pela ADR-028; conferência visual obrigatória nos cinco pontos de quebra ao encerrar cada fase de interface |
| R42 | Dependências npm ampliarem a superfície de cadeia de suprimento | Média | Médio | `package-lock.json` versionado, instalação por `npm ci`, atualização somente após revisão explícita e nenhum carregamento de CDN em runtime |
| R43 | Origem única exigida pela sessão com CSRF ser quebrada por proxy ou novo domínio | Mitigado em 2026-08-03 | Alto | O proxy entrou em 2026-08-03 e publica a **mesma** origem que a aplicação serve, sem domínio separado para o frontend e sem CORS, conforme ADR-052; a origem confiável está declarada em `DJANGO_CSRF_TRUSTED_ORIGINS` e o Django passa a exigir `Origin`/`Referer` porque enxerga HTTPS. Domínio separado ou ambiente HML/PRD continuam reabrindo as ADRs 026 e 014 |
| R44 | Atualização do PrimeNG para 22 ou superior reintroduzir exigência de chave de licença e aviso permanente na interface | Média | Médio | Versão fixada em 21, última sob MIT; atualização passa a exigir decisão de licenciamento e nova ADR, conforme ADR-027 |
| R45 | Dependências transitivas de desenvolvimento do Angular CLI incluírem vulnerabilidade moderada de path traversal no servidor estático Hono para Windows | Baixa no DEV atual | Médio | `npm audit --omit=dev` confirma zero vulnerabilidades de runtime; DEV homologado usa Debian; não executar `npm audit fix --force`, pois a correção proposta altera o Angular CLI de forma incompatível; revisar quando a cadeia Angular publicar atualização compatível |
| R46 | Falha de transporte, bind ou disponibilidade do AD impedir autenticação corporativa | Média | Alto | Escolha única de transporte; LDAPS e CA quando TLS estiver ativo; timeouts, erro genérico sem fallback local comum, probe operacional e superusuário local de contingência |
| R47 | Base ou filtro AD excessivamente amplo expor identidades ou permitir conta fora do público SGPD | Média | Muito alto | Base por OU, grupo obrigatório opcional, suporte a grupo aninhado, filtro fixo revisado, exclusão de conta desabilitada, paginação/limite e autorização administrativa |
| R48 | LDAP simples expor a credencial técnica e a senha dos usuários na rede | Decisão administrativa explícita | Muito alto | Opção exclusiva de SuperAdmin; warning permanente no check e na SPA; conta técnica somente leitura; mesma escolha visível para descoberta e login; recomendar TLS; nunca registrar credenciais |
| R49 | Configuração LDAP pela aplicação expor segredo, aceitar CA inválida ou ativar login sem contingência | Baixa | Muito alto | Acesso direto por `is_superuser`; senha Fernet nunca projetada; upload privado limitado e validado como CA X.509 vigente; hash; versão otimista; auditoria; ativação exige fingerprint de probe e SuperAdmin local utilizável; com TLS, exige CA válida |
| R50 | Setor com escopo ou escalada incorretos gerar tarefa indevida ou bloquear o processo | Média | Alto | Escopo explícito e não redundante, global exclusivo, cadeia de escalada sem ciclos, versão otimista, bloqueio pessimista ordenado do catálogo, service transacional e auditoria antes/depois; os nove setores cadastrados em 2026-07-29 permanecem com atributos provisórios e não devem alimentar o workflow antes da homologação funcional |
| R51 | Dois responsáveis do mesmo setor agirem simultaneamente e duplicarem transição, auditoria, e-mail ou efeito externo | Média | Alto | Mesma autoridade sem coordenador; transação atômica, lock ou versão no estado crítico, chave de idempotência, efeitos após commit e resposta com estado atualizado para quem perder a corrida |
| R52 | Associação de responsável manter cópia divergente do escopo ou sobreviver à desativação/validade do setor ou usuário | Baixa | Muito alto | Colunas duplicadas de escopo removidas; vínculo herda o setor; autorização revalida usuário, setor, associação e vigência; agregação transacional, constraints, revogação lógica e auditoria antes/depois |
| R53 | Confundir papel `DP` com responsabilidade pelo setor Departamento Pessoal e conceder abertura, liberação ou encerramento indevidos | Média | Muito alto | Para usuários funcionais, conceitos independentes e cumulativos e `has_effective_role()` exige `DP` explícito, vigente e no escopo; grupo AD e associação de setor não concedem `DP`; SuperAdmin é exceção global explícita da ADR-044; cada transição revalida estado, prontidão e auditoria |
| R54 | Retry ou concorrência no início duplicar tarefas, checklist ou auditoria | Mitigado em 2026-07-29 | Alto | Lock do processo, versão otimista, chave idempotente vinculada a ator/payload, unicidade por setor e transação única com rollback |
| R55 | Grupos sobrepostos ou configuração alterada gerarem histórico ambíguo | Mitigado no recorte atual em 2026-07-29 | Alto | Mesma versão de template exigida por setor, combinação conservadora de bloqueio/obrigatoriedade, menor prazo e snapshots de grupo, template, setor e perguntas |
| R56 | Código do Checkpoint 4 divergir do schema Oracle enquanto migrations novas não forem aplicadas | Mitigado em 2026-07-29 | Alto | `templates_engine.0001`, `templates_engine.0002`, `templates_engine.0003` e `offboarding.0002` aplicadas; plano vazio, código técnico consistente com o ID, constraints habilitadas/validadas e índice único válido no Oracle DEV |
| R57 | Vincular template a um único setor causar duplicação de questionários e versões divergentes | Mitigado em 2026-07-29 | Médio | Template neutro quanto a setor; associação mantida somente na regra versionada do grupo; mesma versão testada em múltiplos setores e snapshots independentes por tarefa |
| R58 | Edição concorrente de rascunho perder perguntas ou alterar conteúdo histórico | Mitigado em 2026-07-29 | Alto | Somente `DRAFT` é editável; um rascunho por template; lock ordenado do cabeçalho, versões e itens; versão otimista; substituição e auditoria na mesma transação; publicados/aposentados e snapshots permanecem imutáveis |
| R59 | Normalização dos códigos locais colidir, quebrar ordenação ou alterar snapshots históricos | Mitigado no código em 2026-07-29 | Alto | Migrations em duas etapas (`NULL` e depois `ID` decimal), criação transacional pelos services, ordenação por nome/PK em vez de texto numérico, testes de avanço/rollback e nenhuma alteração em snapshots de processos; aplicar no Oracle somente após preflight e revisão do SQL |
| R60 | Resposta escalar de checklist violar a constraint JSON do Oracle ou vazar conteúdo sensível na auditoria | Mitigado em 2026-07-29 | Alto | Documento interno `{"value": ...}` compatível com `IS JSON`, projeção simples na API, validação tipada no backend, smoke Oracle com rollback e auditoria restrita a IDs/quantidade de itens |
| R61 | Comprometimento ou uso indevido de SuperAdmin ampliar acesso a todos os processos e funções | Decisão explícita em 2026-07-30 | Muito alto | Poucas contas ativas, contingência preservada, autenticação forte, proibição de provisionamento implícito, autoridade centralizada por `has_global_authority()`, auditoria com ator, sem bypass de estado/validação/idempotência e revisão periódica das contas SuperAdmin |
| R64 | SuperAdmin comprometido redirecionar o e-mail do sistema para servidor próprio pela central | Baixa | Alto | Autoridade restrita a `is_superuser`; servidor, porta, TLS e remetente gravados na auditoria a cada alteração; senha cifrada e nunca projetada; prova de envio só para o endereço da própria conta; revisão periódica das contas SuperAdmin (R61) |
| R63 | Agendamento parar e a fila de notificações silenciar sem erro visível | Média | Médio | Fila durável que acumula em `PENDENTE` sem perder mensagem; sonda `sgpd_operations_check` e tela `/fe/operacao` declaram a fila parada quando há pendente além da tolerância, com saída diferente de zero para monitor externo; procedimento no `RUNBOOK.md` §2; nenhuma decisão de domínio depende do aviso. Desde a ADR-057 quem agenda é o Beat e quem executa é o worker, ambos sob systemd com `Restart=always`. Sutileza que o corte de 2026-08-06 tornou concreta: **a fila em dia não prova o Beat**, porque o aviso nascido de um ato na tela sai por `on_commit`, a partir do processo web. Com o Beat morto e o worker vivo, a fila drena e só a varredura de prazos deixa de acontecer — o único sinal é o batimento do agendamento em `/fe/operacao`, que ninguém recebe: alguém precisa abrir a tela |
| R65 | Exportação de relatório levar dado pessoal para fora do sistema sem rastro | Mitigado em 2026-08-01 | Médio | Exclusiva de `DP` vigente no escopo ou SuperAdmin; trilha append-only `SGPD_REPORT_EXPORT` com ator, conjunto, período, linhas e correlation ID, gravada antes de o arquivo sair; arquivo sem CPF, motivo, justificativa ou parecer; recorte acima de 5000 linhas recusado |
| R62 | SuperAdmin lançar e aprovar a própria pretensão de cobrança, gerando desconto contra o colaborador sem segundo par de olhos | Aceito por decisão explícita em 2026-07-31 (ADR-048) | Muito alto | Barra absoluta para usuário funcional; decisão exige `DP` vigente no escopo; `segregation_override` gravado na decisão append-only e no evento de auditoria; decisões e trilha imutáveis; consolidação deve permitir separar as decisões marcadas para conferência posterior. A revisão exigida antes de operar fora do DEV único foi feita em 2026-08-05 (ADR-055) e **reafirmou a ADR-048 sem emenda**: travar o SuperAdmin deixaria o sistema sem saída quando não houvesse `DP` vigente no escopo, e a mitigação real é operacional — poucas contas SuperAdmin e revisão periódica (R61) |
| R66 | Senhas fracas ou previsíveis do banco, do SMTP e do ERP darem acesso a dado pessoal a quem apenas adivinhar o padrão | Aceito com prazo em 2026-08-05 (ADR-055) | Muito alto | Identificado em 2026-08-03: `SGPD_DB_PASSWORD` deriva do nome do schema e a do SMTP corporativo segue `empresa+ano`. A troca é feita no Oracle e no Microsoft 365, fora do repositório, e ainda não ocorreu. As credenciais mortas do ERP foram removidas do `.env` na mesma data. Por decisão explícita, não bloqueia o go-live do PRD: enquanto não rotacionadas, valem a leitura restrita do `.env` (600) e o acesso restrito por rede. A rotação da senha do Oracle exige janela combinada com o DBA, e a do SMTP passa pela central (ADR-050), sem reinício |
| R67 | Bind do AD com conta nominal sobre LDAP simples expor a credencial de um usuário real na rede | Aceito com prazo em 2026-08-05 (ADR-055) | Muito alto | Identificado em 2026-08-03: `LDAP_BIND_DN` usa uma conta pessoal e `LDAP_START_TLS` está desligado. Atenuante vigente: `LDAP_AUTHENTICATION_ENABLED=false`, então nenhuma senha de usuário final trafega — só a de bind, usada em descoberta. Correção depende da infraestrutura: conta de serviço somente leitura e LDAPS ou StartTLS com CA, conforme R48 e ADR-030. Enquanto o login AD seguir desligado, o PRD opera com autenticação local; ligar o login AD **antes** da correção transformaria este risco em exposição de senha de usuário final |
| R68 | `runserver` atender o host publicado e degradar sob concorrência ou falhar sem supervisão | Mitigado em 2026-08-05 | Médio | Substituído pelo Gunicorn sob `sgpd-web.service`, com `Restart=always`, recarga graciosa e log no journal (ADR-055). Resíduo declarado: **um worker**, que desde a ADR-057 é escolha operacional e não mais imposição — o cache passou para o Redis compartilhado e a trava só volta a valer se alguém devolver o cache ao processo. A capacidade segue sendo um número conhecido em vez de surpresa, e subir a concorrência exige medir Oracle e pool de conexões antes. `/health/ready/` e a sonda continuam sendo as testemunhas de disponibilidade |
| R69 | Rotação do `DJANGO_SECRET_KEY` tornar ilegíveis os segredos da central de configurações | Média | Alto | A cifra Fernet de `apps/system_settings/crypto.py` deriva do `SECRET_KEY`: trocá-lo invalida a senha de bind do AD e a do SMTP já salvas. Na rotação de 2026-08-03 o custo foi zero porque nenhum ciphertext estava gravado. Com o PRD da ADR-055 apontando para o mesmo schema, o risco deixa de ser só de rotação e passa a ser de migração de host: chave diferente no host novo torna os segredos gravados ilegíveis. Controle: o checklist de corte (`RUNBOOK.md` §11.3) exige a mesma chave ou a reinformação explícita das duas senhas. A saída definitiva é uma chave dedicada e rotacionável, separada da chave de sessão |
| R70 | Um worker do Gunicorn saturar sob concorrência e degradar o tempo de resposta sem que ninguém perceba | Aceito por decisão em 2026-08-05 (ADR-055) | Médio | Oito threads em um processo atendem o volume esperado de um fluxo demissional, mas o teto é conhecido e não elástico. O obstáculo que a ADR-057 removeu foi o do cache: com o Redis compartilhado, o limite de tentativas de login deixou de se multiplicar por worker e o boot aceita `WEB_CONCURRENCY` acima de 1. O que resta antes de subir é medir Oracle e pool de conexões — deixou de ser trava de código e passou a ser dever de medição. Testemunhas: `/health/ready/`, o log de acesso do Gunicorn no journal e o tempo de resposta percebido |
| R72 | DEV e PRD dividirem o mesmo usuário do SO e o mesmo home, permitindo que um comando rodado no diretório errado atinja o ambiente produtivo | Aceito por decisão em 2026-08-05 (ADR-055); premissa reduzida em 2026-08-06 | Alto | O PRD fica em `/home/macari/prd/SGPD` e o DEV em `/home/macari/dev/SGPD`, ambos sob `macari`, porque `/home/macari` é `0700` e uma conta de serviço separada não leria a árvore. Consequências: o `.env` de produção — com a senha do Oracle — é legível pelo shell interativo do desenvolvedor, e nada além do diretório corrente separa um `migrate` de DEV de um `migrate` de PRD. Controles: caminho absoluto nas units e no procedimento de deploy (`RUNBOOK.md` §11.2), `.env` em modo `600` e o gate manual de migration do `scripts/deploy.sh`. A saída definitiva é usuário de serviço dedicado, com a aplicação fora do home. **O corte de 2026-08-06 levou o PRD para outra máquina**, então o home deixou de ser compartilhado e o erro de diretório deixou de existir; o que sobrou do dano — comando de desenvolvimento atingindo dado produtivo — migrou para o R71, pela via do schema |
| R71 | Dois hosts escreverem no mesmo schema `SGPD` | Média | Muito alto | O PRD aponta para o schema que já existe (ADR-055), então nada impede tecnicamente que outro host escreva nele. Sessões concorrentes de hosts diferentes escapariam do raciocínio de concorrência do domínio, que assume um processo. O corte de 2026-08-06 fechou a janela aguda — o serviço do host anterior foi parado antes de o proxy ser reapontado (`RUNBOOK.md` §11.3) —, mas o risco deixou de ser pontual e virou permanente: **o host anterior segue como DEV apontando para o mesmo schema, agora produtivo**, com `DJANGO_SETTINGS_MODULE=config.settings.production` no `.env` e o mesmo Oracle. Qualquer `manage.py`, worker ou Beat levantado no DEV atinge dado real. Controle atual: nenhum serviço do SGPD roda no DEV e o `crontab` está desativado — disciplina, não barreira. A saída definitiva é schema próprio para o DEV |

## Riscos encerrados

| ID | Risco | Encerramento |
|---|---|---|
| R34 | Runtime HTMX local ficar desatualizado ou divergir da origem oficial | Encerrado em 2026-07-28 após remoção do runtime na Fase G |
| R40 | Remover a interface server-side antes da SPA cobrir a função equivalente | Encerrado em 2026-07-28; a remoção ocorreu somente após a cobertura funcional da SPA |
